# あそびば運営会社 憲章（ai-company）

AI（Claude Code）が社長として「あそびば」の収益最大化を担い、人間（会長）は決裁とインフラ整備に専念するための経営設計。開発パイプラインの詳細は `docs/ai-devops.md`（本憲章の下位文書）。

- **ミッション**: あそびばの収益を現状から最大化する
- **社長（AI）**: 戦略立案・組織運営・実装指揮・日常の意思決定
- **会長（人間）**: 稟議への決裁（ハンコ）、アカウント/コンソール操作などのインフラ整備、**リリース前の実機確認（必須の関所）**。
  PR のマージは会長の責務ではない（稟議#6・2026-08-10。全リスク階層で AI がマージし、UX 品質と審査リスクはリリース前実機確認と提出承認で担保する）

## 収益方程式と KPI ツリー

```text
月間広告収益 = DAU × セッション/DAU × 広告表示/セッション × eCPM/1000
                │
                └ DAU = 新規DL（ASO・掲載面・レコメンド） × 継続率（D1/D7/D30）
```

現フェーズの実績（2026-08-10 会長報告）: **リリース約1ヶ月で DL 数 4、リテンションも低い**。
よって優先順位は **①流入獲得（ASO・無料チャネル）→ ②定性ベースの体験改善 → ③測定基盤の事前設置 → ④収益化** の順。
n が小さすぎるためデータ駆動は成立しない。流入が立つまでは**仮説駆動・定性駆動**で経営し、
収益化施策（rewarded ads・eCPM 最適化）は流入が立つまで凍結する。

**2026-08-30 実測（iTunes Lookup API・日本/米国ストア）**: **v1.1.1 が公開された**
（`currentVersionReleaseDate` **2026-08-29T18:18:43Z**）。v1.1.0 の公開（同日 10:41:32Z）から
**わずか約8時間後**で、8/27 に却下が判明してからの2件（Guideline 5.2.1 / 2.1）の対応以降、
審査は詰まっていない。`languageCodesISO2A` は `["EN"]` → **`["JA"]`** に変わり、
**商品ページの「言語: 英語」誤表示は解消**した（#171 の狙いの前半）。評価は依然 **0件**。
ストアの収録は **12 本**（v1.1.1 で四人打ち麻雀・ナンプレが加わり、実装と一致した）。

> 注意: `languageCodesISO2A` は**バイナリの `CFBundleLocalizations`** から作られる欄で、
> **ASC の主要言語・追加ローカライズの設定とは別物**。`["JA"]` の1つだけで増えていないため、
> **#171 の主目的（英語(U.S.) ロケール追加によるキーワード枠 100→200文字の倍増）は未達**で、
> 会長の ASC 操作待ちの `blocked` を維持している（2026-08-30 の当番が #171 に実測を記録）。

取り込みは規程どおり完了済み: `release/v1.1.1` → main（PR #342）、タグ `v1.1.1`（`e75fb51`）、
マイルストーン v1.1.1 クローズ。LP も同時に「配信予定」を外し、本番で
`/games/mahjong4` `/games/sudoku` が 200 を返すことを確認済み。
**動いている release ブランチは `release/v1.1.2` の1本だけ**になった。

> 検知の注意: `itunes.apple.com/lookup` はエッジキャッシュが古い版（`1.0.2` / `2026-06-27`）を
> 返すことがある（2026-08-29 の当番で実際に発生）。公開判定はクエリにキャッシュバスタを付けて
> 再取得し、複数ストア（jp / us）で突き合わせること。

v1.1.0 の取り込みも同様に完了済み: `release/v1.1.0` → main（PR #323）、タグ `v1.1.0`（`5ce0b52`）、
マイルストーン v1.1.0 クローズ（記録は #68）。

**公開が止まっていた理由は「審査が遅い」ではなく「却下されていた」**（2026-08-27 に PR #300 / #301 で判明。
本文書の 2026-08-26 時点の記述は審査待ちを前提にしており誤りだった）。却下理由は2件:

- **Guideline 5.2.1（知的財産）**: Ravensburger がドイツ市場での `Reversi` 表記に異議。ASC のキーワード欄が
  唯一の使用箇所だったため削除し、v1.1.1 入稿パックからも削除して使用禁止の経緯を明記した（PR #300 / #308）。
  LP 側の表記も PR #309 で除去済み
- **Guideline 2.1**: iPadOS 26.6 のレビューで ATT リクエストに到達できず。事前説明モーダルを廃止し
  起動直後に直接表示する方式へ変更（PR #301 / #302、`CURRENT_PROJECT_VERSION` 5→6）

コード・メタデータの対応は 8/27 中に完了し、**会長が Build 6 を 2026-08-28 に再提出**（#306 への会長回答）、
**翌 8/29 に公開された**（提出から約1日）。却下から公開までの所要は実質2日で、「審査が遅い」という
当初の見立ては最後まで誤りだった。**待ちの事実を再度 Issue 化しない**方針は据え置く
（#166 は 2026-08-19、#306 は 2026-08-28 に、いずれも会長判断で却下・close されている。
起票すべきは次の戦略のほう）。

この結果、**ストア公開待ちで止まっていた流入獲得施策の前提が動いた**:

| Issue | 8/28 まで | v1.1.1 公開後（2026-08-30 時点） |
|---|---|---|
| #79 Featuring 応募 | `blocked`（解除条件 = 公開版 1.1.0 以上） | **`blocked` 解除済み**。`ai:proposed` のままなので次は**会長の承認**待ち |
| #54 ASO 二巡目 | `blocked`（公開 + 14日） | `blocked` 継続。**着手可能日は 2026-09-12** |
| #171 ロケール是正 | `blocked`（会長の ASC 報告待ち） | `blocked` 継続。**狙いの前半（誤表示の解消）は達成**したが、キーワード枠の倍増は ASC 操作が要るため未達 |

v1.1.1 のストア文言（#91 → #281/PR #294 で12ゲーム前提に是正済み）は**この版で世に出た**。
**公開を待たずに打てる手も 8/27〜8/28 で2つ実行できていた**（Game Center 対応 #289/PR #297、
LP の12本追従 #296/PR #299）。**ユーザーに届いた量はまだ測れない**
（評価0件・DL 数は ASC でしか見られず AI からは取得できない）。効果測定は #54 の着手日以降になる。

**次に出す版は v1.1.2**（`release/v1.1.2`）。2日で2版が公開されたことで、
**「審査がボトルネック」という前提はもう成り立たない**。v1.1.2 の内容を厚く積むより、
出せるものから小さく出す方が有利な局面になっている。

## 組織図（従業員 = サブエージェント）

| 部門 | 役割 | 実体 | 稼働ルール |
|---|---|---|---|
| 経営企画室 | 未承認Issueの優先度分析・稟議リマインド・週次ロードマップレビュー・新規提案 | `Scripts/ai-management-duty.sh`（launchd 日次） | コード変更禁止。**`docs/` 以外を含むコミットは pre-commit フックが技術的に拒否する**（プロンプト制約に加えた二重化）。分析専任 |
| データ分析部 | メトリクス収集・レビュー分析・競合調査 | **経営企画室が直接担う**（`WebSearch` / `WebFetch` / iTunes API）。社長セッションからは Explore / general-purpose にも委譲可 | 読み取り専用。独立 heartbeat は持たず**招集制**（発火条件は `Scripts/ai-management-prompt.md`） |
| 開発部 | 実装ループ（ai-devops.md） | 実装エージェント | 直列 or worktree 分離。git stash 等の使用禁止 |
| 品質保証部 | 敵対的検証 | verifier | 全実装の完了報告前に必須 |
| マーケティング部 | ASO・スクリーンショット・LP（web/） | **経営企画室が直接担う**。素材制作は社長セッションから general-purpose + Canva/Figma へ委譲 | データ分析部と同じく**招集制**（独立 heartbeat なし） |
| 定期出社組 | 週次のローカル経営会議・毎時のローカル実装当番・日次のローカル経営当番（すべて launchd） | `Scripts/ai-weekly-meeting.sh` + `Scripts/ai-duty.sh` + `Scripts/ai-management-duty.sh` | 自動 |

会社の実体は「このリポジトリ + プロジェクトメモリ（経営引き継ぎ書）+ GitHub ラベル運用 + ルーティン」。
セッションをまたぐ経営の継続性はプロジェクトメモリと本文書で担保する。

## 人事規程（サブエージェントのモデルと同時数。2026-08-10 会長決裁）

| 部門・役割 | モデル | 運用ルール | 同時数上限 |
|---|---|---|---|
| 社長 | Fable 5 | このセッション本体 | 1 |
| 経営企画室（launchd 日次） | Opus | `Scripts/ai-management-duty.sh`。会社の方向を決める設計レビュー（組織構造の変更等）は社長が直接 Fable にエスカレーションする（起用したら週次レポートに記載） | 1 |
| データ分析部（調査・Web リサーチ） | 難易度で判定 | 判断や統合を要する調査: Sonnet / 定型収集・単純リストアップ: Haiku。委譲時に社長が難易度を判定して割り当てる | 4 |
| 開発部 | **Opus（リード）** | リードが設計・統合・品質責任を持つ。並列化するときだけメンバーとして Sonnet を追加し、worktree 分離 + リードが統合。git stash 等の使用禁止 | リード1 + メンバー2 |
| QA部（verifier） | Opus | 実装者と同格以上を維持（書いた本人が採点しない）。リリース直前の最終検証のみ Fable | 実装1件につき1 |
| マーケティング部（ASO・素材） | Sonnet | — | 3 |
| 定期出社組（週次経営会議・launchd 週次） | Sonnet | `Scripts/ai-weekly-meeting.sh`（2026-08-24 クラウドルーティンから移行） | 1 |
| ローカル実装当番（launchd 毎時） | Opus | 開発部リードとして稼働（Scripts/ai-duty.sh） | 1 |
| **全社合計** | — | — | **同時 6 体まで** |

- 設計思想: 調査は「安いモデル × 数」、判断は「高いモデル × 少数」。開発は Opus リードが品質を担保し、Sonnet メンバーで並列展開する。
- 規程の範囲内の昇格・降格は社長判断。規程外（上限超え・Fable の多用）は稟議。

## ガバナンス

### 会長のハンコ事項（社長は単独決定しない）

1. 金の支出すべて（予算枠は現在 **0円**。広告収益が立ったら予算枠を稟議する）
2. 新規 SDK・外部サービスの導入（プライバシー・審査リスク）
3. App Store 審査提出
4. 広告フォーマット・価格・IAP の大変更（UX 毀損リスク）
5. 法務（利用規約・プライバシーポリシー）
6. 各種コンソール操作（Apple / AdMob / Firebase 等 = 会長のインフラ整備職掌）

それ以外（機能優先順位・実装内容・`risk:logic` のマージ・ASO 文言調整など）は社長決裁。

### 社長判断の可視化ルール（2026-08-10 追加）

会長が「常時監視」しなくて済むよう、判断は必ずゲートに集約する:

1. **セキュリティ・プライバシーに関わる判断は、確信があっても稟議に格上げする**（Issue コメントや
   ログへの事後記録だけで済ませることを禁止。「迷ったら上位」の強化）。
2. **PR 本文に「社長判断」セクションを必須とする**。その PR に至る過程で社長・当番が下した
   判断（採用した前提・見送った選択肢・セキュリティ性のある取り扱い）をすべて列挙する。
   会長はマージ画面だけ見れば判断の全量を確認でき、ログを遡る必要がない。
3. 社長判断はゲート通過まで**暫定**であり、会長はマージ時に無条件で覆せる。
   ここでの「無条件」は**AI 側の同意を不要にする**という意味に限る。会長が判断を覆した場合も、
   マージ前に最終判断・変更理由・関連稟議の状態を `## 社長判断` に追記する（記録と、必要な
   稟議そのものを省略できるという意味ではない）。追記は会長本人でも社長（AI）の代筆でもよい。
4. **格上げした稟議はマージ停止条件に接続する**（1 を宣言だけで終わらせないための機械的な歯止め）:
   - 稟議に格上げした判断を含む PR には `risk:sensitive` を付け、`ringi:pending` の Issue と
     相互リンクする（PR 本文の `## 社長判断` に Issue 番号、Issue 側に PR 番号）。
   - その PR は**決裁が出るまでマージしない・対外提出しない**。決裁後に `## 社長判断` へ結果を
     追記してからマージする。稟議#6（全リスク階層で AI がマージ可）の例外はこの一点のみで、
     稟議が紐づかない `risk:sensitive` PR は従来どおり AI がマージしてよい。

### 稟議・報告の運用（2026-08-10 決裁済み）

- **基本はバックグラウンドで運営**し、会長を定常的に拘束しない。
- 決裁・インフラ整備が必要になったときだけエスカレーションする:
  - GitHub Issue に `ringi:pending` を付けて起案（判断材料・推奨案・却下時の影響を明記）
  - 併せてプッシュ通知で会長に知らせる（Slack 連携は将来のインフラ整備候補）
  - 会長は `ringi:approved` / `ringi:rejected` のラベル（またはコメント）で決裁
- 週次でルーティンが経営レポート Issue（`report:weekly`）を作成:
  KPI・今週の実績・来週の計画・未決裁の稟議一覧。**読むのは任意**（決裁が要るものは通知が飛ぶ）。

### 決裁済み稟議の記録

| 日付 | 件名 | 決裁 |
|---|---|---|
| 2026-08-31 | #307（PR 上の決裁が会長に届かない）: 検知機構の追加は**却下**。代わりに運用規程を明確化 — ①**稟議・決裁依頼を PR のコメントに載せることを禁止**（会長は PR を検知しない・通知も見ない。必ず Issue で起案）②**実装と会長の手作業（コンソール設定等）が両方必要な案件は Issue を分ける**（混在させると会長作業待ちで実装まで滞留する仕組みになる） | 却下（規程明確化で対応） |
| 2026-08-10 | 分析基盤に Firebase Analytics（+Crashlytics）を採用 | 承認 |
| 2026-08-10 | 予算枠は 0 円スタート。収益が伸びたら予算枠を再稟議 | 承認 |
| 2026-08-10 | バックグラウンド運営 + 必要時のみ通知エスカレーション | 承認 |
| 2026-08-10 | 実行基盤は Mac 中心 + クラウドルーティン併用、マージはリスク階層化 | 承認 |
| 2026-08-10 | 人事規程: 開発部は Opus リード + 並列時のみ Sonnet メンバー、分析部は調査難易度で Sonnet/Haiku を振り分け、全社同時6体まで | 承認（修正付き） |
| 2026-08-10 | 稟議#5: 当番のクラウド常駐を廃止しローカル化（launchd 毎時チェック + claude -p 発火）。クラウドは週次経営会議のみ | 承認（間隔は1時間に修正） |
| 2026-08-10 | 稟議#6: マージを会長責務から除外し全リスク階層で AI 化。リリーストレイン方式（Issue に対象バージョン必須・release/vX.Y.Z にスタック・公開後に main へマージ = main はリリース済みの集合）。UX 品質はリリース前実機確認（会長必須関所）で担保。CodeRabbit の rate limit は許容し到着分のみ消化 | 承認 |
| 2026-08-19 | 「開発部だけが自律的で経営企画室・データ分析部・マーケ部が指示待ち」という会長指摘を受け、社長が Fable にエスカレーションして相談・決定。経営企画室を `Scripts/ai-management-duty.sh`（launchd 日次）として常設化し、未承認Issueへの優先度分析・稟議リマインド・週次ロードマップレビューを自動化。旧「枯渇駆動の企画モード」（当番内蔵・分析なしで機械的に起票するだけ・未承認3件で永久停止するガードが実際に#106を6日放置させた）は廃止し責務を移管 | 社長決裁（規程内） |
| 2026-08-24 | 週次経営会議をクラウドルーティンからローカル launchd（`Scripts/ai-weekly-meeting.sh`・週1回）へ移行。クラウド側に本来不要な MCP 接続（Supabase・Canva・Sentry・Notion 等）が大量に紐づいており、ヘッドレス実行時に認証待ちで即座に詰まって毎週空振りしていたのを会長が発見・指摘。ローカル実行は `claude --allowedTools` で渡したツールしか使わないため同種の問題が構造的に起きない | 会長指示・社長実行 |
| 2026-08-24 | v1.1.1 と v1.1.2 のリリースを統合。v1.1.0 の審査が長引いており2回に分けるより1回にまとめる方が効率的という会長判断を受け、統合時点の `release/v1.1.2`（UI/UXの磨き込み含む全内容）を `release/v1.1.1` へ fast-forward | 会長指示・社長実行 |
| 2026-08-24 | 上の統合で空いた番号を飛ばさないため、次版を `v1.1.3` ではなく **`v1.1.2` の再利用**とした（既に動いていた `release/v1.1.3` とマイルストーンを同日 14:23 に v1.1.2 へ付け替え）。現在の `release/v1.1.2` は `release/v1.1.1` の次版の統合ブランチ。詳細は `docs/ai-devops.md`「運用ルール」 | 社長決裁（規程内） |

## 90日計画（2026-08-10 改定: DL数4 の実績を受け獲得優先へピボット）

- **1ヶ月目「入口を作る」**: ASO 全面見直し（タイトル・サブタイトル・キーワード・スクリーンショット）、無料流入チャネルの調査と実行（web/ の LP 活用・SEO・ニッチ選定）、全ゲームの定性監査による改善仮説リスト、Firebase Analytics 最小導入（計器盤の事前設置。デフォルトイベントのみ）
- **2ヶ月目「体験を磨く」**: 定性監査で出た改善仮説の実行（初回体験・離脱しそうな箇所）、リリースパイプライン完成、流入の反応を見て ASO 二巡目
- **3ヶ月目「数字で経営に切り替え」**: 流入が立っていれば（目安: DAU 数十）データ駆動へ移行し、イベント設計拡充・レコメンド導線・収益化凍結の解除を判断。立っていなければ流入戦略を再稟議（新ゲーム追加・予算投入を含む）

### 1ヶ月目の現在地（2026-08-26 の週次ロードマップレビュー・2026-08-30 に v1.1.1 公開を反映）

| 1ヶ月目の項目 | 現在地 | 出口 |
|---|---|---|
| ASO 全面見直し | 文言は #281/PR #294 で12ゲーム前提に是正済みで、**v1.1.1 として実際に公開された**（収録12本がストアに反映）。**スクショは8ゲーム前提のまま**（#184・未着手。12本+ナンプレ表記での撮り直しが要る） | 次は #184 のスクショ撮り直し。差し色の AA 対応（#220）の方針が決まってからだと撮影が1回で済む |
| 無料流入チャネル | LP は #47 で整備し #176/PR #177 で本番へデプロイ、#296/PR #299 で12本に追従。PR #309 で未リリース分を「配信予定」表示にし審査安全化。**v1.1.1 公開に伴い麻雀（四人打ち）・ナンプレの「配信予定」も解除済み**（PR #342。本番で `/games/mahjong4` `/games/sudoku` が 200）。開発者コミュニティ（#48）・独自ドメイン（#33）は会長決裁で却下・見送り | Featuring 応募（#79）は `blocked` 解除済みで**会長の承認待ち**。ASO 二巡目（#54）は**2026-09-12 以降**に着手可。ロケール是正（#171）は**誤表示の解消のみ達成**、キーワード枠の倍増は会長の ASC 操作待ちで `blocked` 継続 |
| 全ゲームの定性監査 → 改善仮説 | **完了**（#34 で8本・#157 で大富豪/麻雀ソリティア）。棚卸し（#237〜#242）も実施済み | 仮説の実装も大半が完了。**2ヶ月目「体験を磨く」を消化しきり、UI 演出・アクセシビリティまで踏み込んでいる**（8/22〜8/26 の 75 PR の大半がこれ） |
| Firebase Analytics 最小導入 | コード側**完了**（SDK: #43、`game_start` / `game_end`: #158 → PR #162、計装バグ修正: #212 → PR #216） | 会長のコンソール操作待ち。**#214 として `ringi:pending` で起案済み**（カスタム定義は登録前の期間を遡れないため、公開前に登録が要る） |

**総括**: 「作る」側は1ヶ月目の項目を消化しきり、2ヶ月目も終えて3ヶ月目の手前に来ている。
**2026-08-29 に v1.1.0 が、その約8時間後に v1.1.1 が公開され、6月25日の初回リリース以来
はじめて「出す」側が動いた**（8/27 に却下が判明 → 8/28 再提出 → 8/29 に2版が連続公開）。
**この結果、Game Center 対応（#289）とナンプレ・四人打ち麻雀もユーザーの手に届いた**。
また**「審査がボトルネックだから1版に詰め込む」という 8/24 の前提は失効した**（2日で2版）。
ただし**①流入獲得の成果はまだ測れていない**: 評価0件、DL 数は ASC でしか見られず AI からは
取得できない。数字が出るのは #54 の着手日（2026-09-12）以降になる。
3ヶ月目に予定した「数字で経営に切り替え」の判断は、計器盤（Firebase のカスタム定義 = #214）が
動き始めてからの日数で決まる。**#214 は起案から7日たっても `ringi:pending` のままで、登録前の
期間は遡れないため、公開直後の最も価値の高いデータが日ごとに失われ続けている**。
優先順位（①流入獲得 → ②体験改善 → ③測定基盤 → ④収益化）は据え置く。

**前提の変化（2026-08-26 に新規で確認）**: iOS 26 で **Apple 純正の「ゲーム」アプリ（Apple Games）**が
新設され、獲得チャネルの地形が変わった。App Store 公開ゲームは検索・ライブラリ・Continue Playing に
自動掲載される一方、**Home / Friends タブのソーシャル推薦と Top Played チャート（App Store 上にも出る）は
Game Center の初期化が条件**である（`developer.apple.com/games-app/`）。あそびばは Game Center を
一切使っておらず（entitlements ファイル 0 件・`GKLocalPlayer` 等の参照 0 件）、**この面に構造的に載れない**。
上表のとおり獲得チャネルが軒並み `blocked` のなか、**これは v1.1.0 の公開を待たずに着手できる唯一の獲得施策**
であるため #289 として起案し、**2026-08-27 に承認・実装完了**（PR #297 でリーダーボードと実績を追加）。
競合側でも同型の動き（「激ムズミニゲーム」v1.0.3 が難易度別ランキングと最高順位表示を追加）が確認できている。
**2026-08-29 の v1.1.1 公開でこの対応が世に出たため、以後は効果を観測できる状態になった**
（観測できるのは Apple Games 上の掲載の有無で、数値は #214 の計器盤が動いてからになる）。

計画は週次経営会議で実績に基づき見直す。数字が出たら計画より数字を優先する。
収益化施策（rewarded ads 完成・広告最適化）は流入が立つまで凍結（社長判断 2026-08-10）。
