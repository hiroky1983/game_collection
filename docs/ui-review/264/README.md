
## 4f9e2dc（会長の修正を全部取り込んだ HEAD）の再実測 — 2026-08-25 16:2x〜16:4x JST

決裁スレッドが前提にしていた `5ac5225` から、会長が `1a0e6b3`（河を常に2列に制限）・
`73a5476` / `e722297`（副露のはみ出し・押し出し）まで進めたので、**決裁が今も必要かを確定
させるため**に現 HEAD `4f9e2dc` を測り直した。結論は「必要。しかも前提が2点変わっている」。

### 測り方（再現手順）

`4f9e2dc` を素のままビルドし（コード変更なし）、`make-snapshot.py` が作る中断スナップショットを
シミュレータのコンテナへ置いて `-startGame mahjong4` で復元する。手でここまで打たなくても
最悪局面を確実に再現できる。

```bash
python3 make-snapshot.py <副露の組数> <立直するか 0/1> \
  > "$(xcrun simctl get_app_container <UDID> com.hirockysan1983.asobiba data)/Library/Application Support/Snapshots/mahjong4.json"
xcrun simctl launch <UDID> com.hirockysan1983.asobiba -startGame mahjong4
```

人間（席0）だけ非立直にしてあるのは、立直中は自摸切りで局が勝手に進んでしまい
状態を固定できないため。会長が `4f9e2dc` で足した `-mahjongAutoPlay` は、
通常対局を自動進行させる確認（`残り48` → 東風戦終了まで）にも使った。

### 結果

- `4f9e2dc-se-head.jpg` — iPhone SE (3rd gen) / iOS 26.4。副露0・1組・4組
- `4f9e2dc-se-zoom.jpg` — 上家（左）の拡大。重なりの現物
- `4f9e2dc-17pro.jpg` — iPhone 17 Pro / iOS 26.4
- `4f9e2dc-probe-ab.jpg` — 選択肢 A+B を入れた**一時ビルド**（コミットしていない）の効き

| 条件（河は満杯・CPU3人立直） | iPhone SE | iPhone 17 Pro |
|---|---|---|
| 副露0組 | **重なる**（上家・下家のチップに河の1段目が乗り、立直タグが完全に隠れる） | **重なる**（同じ） |
| 副露1組 | 重なる（副露・チップ・河が三つ巴） | — |
| 副露4組 | 重なる + 対面チップが卓の上辺で欠け、自分の河が下辺で切れる | — |

**前提の変化1: SE 固有ではなくなった。** 決裁スレッドの表は「17 Pro は牌を縮めれば機械的に
解ける／SE だけ 50pt 足りない」としていたが、現 HEAD では 17 Pro でも同じ重なりが出る。
`t = side/19` として卓が要求する高さを積むと `188 + 10.72t` に対し使えるのは `19t` で、
収まる条件は `side ≥ 431pt`。17 Pro の卓は約 367pt なので約 28pt 足りない（SE はさらに不足）。

**前提の変化2: #263（鳴き）が入って要素が増えた。** 決裁の A/B/C は副露が無い時代の
選択肢で、副露が2組以上ある局面はどれを選んでも救えない（下記）。

### 選択肢の効きを一時ビルドで実測した

- **`LazyVGrid` の高さ過少申告ではない**（当番の仮説は外れ）。`discardStrip` を素の
  `VStack`+`HStack` に置き換えても重なりは1ピクセルも変わらなかった。**素直に場所が足りていない**。
- **B のみ**（左右の河を4段→2段 = 表示8枚→4枚）: 左右のチップの重なりは解消するが、
  **自分の段が残る**（名前チップに河の1段目が乗り、河の2段目が手牌の縮小一覧に隠れる）。
- **A + B**（手牌の縮小一覧を出さない + 左右の河を2段）: **副露1組までは SE・17 Pro とも完全に収まる**。
  副露2組で自分の副露と河が再び重なり、4組では卓外へ切れる。
- **C（中央パネルを出さない）は高さに効かない**。中央パネルは左右の列と同じ行に居るので、
  外しても行の高さ（= 左右の列の高さ）が縮まない。決裁の推奨 A+C は、実測では A+B であるべき。

したがって当番の推奨は **A+B に加えて、副露の表示自体にも上限か圧縮が要る**。決裁の
選択肢に無い項目なので、決裁スレッドを更新して再掲した。

## db93b60（コンフリクト解消後の HEAD）の iPhone SE 実測 — 2026-08-25

`db93b60`（PR #264 の現 HEAD）をビルドし、河が満杯・全員立直の最も厳しい局面を
iPhone SE (3rd generation) / iOS 26.4 で撮り直した。

`db93b60-iphone-se.jpg`

**上家・下家（左右）の名前チップが河に重なる問題は解消していない**。左右のチップは
CPU アイコンと「立直」バッジしか読めず、名前と点数が河の 1 段目に隠れている。
加えて、自分の河の最下段が卓の下辺からはみ出している。

したがって [2026-08-25 03:16 の決裁スレッド](https://github.com/hiroky1983/game_collection/pull/264#issuecomment-5400228752)は
まだ有効で、当番はこの PR をマージしていない。

## #263（鳴き）を取り込んだあとの実測 — 8da9c9a・2026-08-25

`release/v1.1.1` に #272（麻雀の鳴き・#263）が入ったことでこの PR と衝突したため、当番が
`8da9c9a` で解消した。**雀卓レイアウトはこの PR のものをそのまま採用し、#263 の UI を
こちらの構造へ置き直している**。iPhone SE (3rd generation) / iOS 26.4 で実測:

- `8da9c9a-melds-iphone-se.jpg` — 各家の副露が名前チップの下に並び、自分の副露は卓下の手牌の帯の下に出る。カンできるので「カン」ボタンが出て、鳴いたので「立直」が押せない
- `8da9c9a-call-iphone-se.jpg` — 鳴きの提示（ポン / スルー）が卓の下に出る

**左右のチップが河に重なる問題（上の節）はこの解消では触っていない**。決裁待ちのまま。
